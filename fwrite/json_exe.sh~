#!/bin/bash

for file in ./xmls/*
do
    name=$(basename ${file} .xml)
    echo ${name}
    xml2json -t xml2json -o ${name}.json ${file}
    mv ${name}.json ./jsons/${name}.json
    cd ./jsons
    ~/dropbox_uploader.sh upload ${name}.json ./jsons/fwrite/${name}.json
    cd ../
done


    
